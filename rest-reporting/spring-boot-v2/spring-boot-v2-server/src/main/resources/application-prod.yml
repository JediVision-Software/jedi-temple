# Server
server:
  port: 8228

# Logging
logging:
  config: classpath:logback-prod.xml

# Application Configuration
appConfigs:
  cors:
    enabled: true
    allowedOrigins:
      - http://localhost:8686
      - http://localhost:8787
